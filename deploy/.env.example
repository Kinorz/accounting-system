# ===== App =====
ASPNETCORE_ENVIRONMENT=Production

# Public URL used for ASP.NET Core forwarded headers / link generation if needed
PUBLIC_BASE_URL=https://example.com

# ===== Postgres =====
POSTGRES_DB=accounting
POSTGRES_USER=accounting
POSTGRES_PASSWORD=change-me

# Connection string consumed by the API container
# Example uses Docker network host name "db".
CONNECTIONSTRINGS__DEFAULT=Host=db;Port=5432;Database=accounting;Username=accounting;Password=change-me;Pooling=true;Minimum Pool Size=0;Maximum Pool Size=50;

# ===== JWT (required for /auth/*) =====
# NOTE: Use Jwt__* (double underscore) for environment variables.
# Issuer/Audience can be your public base URL in production.
Jwt__Issuer=https://example.com
Jwt__Audience=https://example.com
# Use a long random secret (32+ chars). Never commit real values.
Jwt__SigningKey=change-me-to-a-long-random-string-at-least-32chars
Jwt__AccessTokenMinutes=60
